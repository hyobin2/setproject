<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.mobinus.service.member.impl.MemberMapper">

	<select id="select" resultType="myCamelMap">
		<![CDATA[
			SELECT
				M_IDX,
				ID,
				NAME,
				PW,
				EMAIL,
				AUTH,
				USE_YN,
				REG_ID,
				REG_NAME,
				REG_DATE
			FROM
				TB_MEMBER
			WHERE
				USE_YN = 'Y'
			AND
				ID = #{id}
		]]>
	</select>

</mapper>
